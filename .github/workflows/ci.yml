name: VS Code Extension CI Gate

on:
  push:
    branches: [ "main", "feature/*" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-and-test:
    name: Typecheck, Build, Test & Package VSIX
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"

      - name: Install Dependencies
        run: npm ci

      - name: Typecheck
        run: npm run typecheck

      - name: Run Multi-Protocol Tests
        run: npm test

      - name: Build Extension Bundle
        run: npm run build

      - name: Package VSIX (Dry Run)
        run: npx @vscode/vsce package --no-git-tag-version
