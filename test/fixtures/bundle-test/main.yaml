openapi: 3.0.3
info:
  title: Main API
  version: 1.0.0
paths:
  /users:
    get:
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "./ref.yaml#/components/schemas/User"
